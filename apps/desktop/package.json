{
  "name": "@rakazo/desktop",
  "productName": "Rakazo",
  "version": "0.1.2",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "bun run build && electron .",
    "build": "tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "pack": "bun run --filter @rakazo/web build && bun run build && electron-builder --mac --win --linux",
    "pack:dir": "bun run --filter @rakazo/web build && bun run build && electron-builder --dir",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --root ../.. apps/desktop/src",
    "test:e2e": "bun run build && playwright test --config e2e/playwright.config.ts",
    "release": "bun run --filter @rakazo/web build && bun run build && electron-builder --publish never"
  },
  "dependencies": {
    "@rakazo/contracts": "workspace:*",
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@rakazo/ui-tokens": "workspace:*",
    "electron": "^43.4.0",
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  },
  "build": {
    "appId": "dev.rakazo.desktop",
    "directories": {
      "output": "out",
      "buildResources": "assets"
    },
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../web/dist",
        "to": "web"
      },
      {
        "from": "../../infra/compose/docker-compose.images.yml",
        "to": "stack/docker-compose.images.yml"
      },
      {
        "from": "../../infra/compose/.env.images.example",
        "to": "stack/.env.images.example"
      }
    ],
    "extraMetadata": {
      "main": "dist/main.js"
    },
    "mac": {
      "icon": "assets/icon.icns",
      "category": "public.app-category.productivity",
      "target": [
        "dmg",
        "zip"
      ],
      "notarize": true
    },
    "win": {
      "icon": "assets/icon.ico",
      "target": [
        "nsis"
      ],
      "verifyUpdateCodeSignature": true
    },
    "linux": {
      "executableName": "rakazo",
      "icon": "assets/icon.png",
      "target": [
        "AppImage"
      ]
    },
    "publish": [
      {
        "provider": "github",
        "owner": "elie222",
        "repo": "rakazo"
      }
    ]
  }
}
